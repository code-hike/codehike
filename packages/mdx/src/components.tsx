import {
  Section,
  SectionLink,
  SectionCode,
} from "./section"
import { Code } from "./code"
import { CodeLink } from "./links"

export const CH = {
  Code,
  Section,
  SectionLink,
  SectionCode,
  CodeLink,
}
